package com.ll.demo.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.ll.demo.entity.DBFile;

public interface FileMapper extends BaseMapper<DBFile> {
    // 这里可以根据需要添加自定义的方法
}
